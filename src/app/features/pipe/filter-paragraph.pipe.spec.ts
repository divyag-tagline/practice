import { FilterParagraphPipe } from './filter-paragraph.pipe';

describe('FilterParagraphPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterParagraphPipe();
    expect(pipe).toBeTruthy();
  });
});
