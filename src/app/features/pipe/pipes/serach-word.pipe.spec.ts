import { SerachWordPipe } from './serach-word.pipe';

describe('SerachWordPipe', () => {
  it('create an instance', () => {
    const pipe = new SerachWordPipe();
    expect(pipe).toBeTruthy();
  });
});
